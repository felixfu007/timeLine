# Add project specific ProGuard rules here.
# M1: minifyEnabled is false for both debug and release builds, so these rules are not
# exercised yet. Revisit before the first Play Store release build (M8) — in particular add
# keep rules for Moshi-generated JSON adapters and Room entities if minification is enabled.
